/**
 * create_document — initialize a new research-studio document.
 * See config/reference/document-dsl.md for the DSL spec.
 */
import { callCreateDocument } from './document-io.mjs';
export const call = callCreateDocument;
